[project]
name = "ducklake"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[duckdb]>=1.11.0",
    "duckdb>=1.3.0",
    "enlighten>=1.14.1",
    "sqlmesh[dlt,github,lsp,postgres,web]>=0.188.1",
]
